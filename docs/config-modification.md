# Config

## Overview

This file describe what options are available in `ConfigInterface`.

## `ConfigInterface`

``` typescript
{
    show?: {
        info?: boolean,
        security?: boolean,
        servers?: boolean,
        topics?: boolean,
        stream?: boolean,
        events?: boolean,
        messages?: boolean,
        schemas?: boolean
    },
    disableDefaultTheme?: boolean
}
```