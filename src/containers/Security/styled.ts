import { styled } from '../../common';

export const Security = styled.div`
  ${props => props.theme.security}
`;

export const SecurityHeader = styled.header`
  ${props => props.theme.securityHeader}
`;
