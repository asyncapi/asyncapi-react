import { styled } from '../../common';

export const Schemas = styled.div`
  ${props => props.theme.schemas}
`;

export const SchemasHeader = styled.header`
  ${props => props.theme.schemasHeader}
`;

export const Schema = styled.div`
  ${props => props.theme.schema}
`;

export const SchemaHeader = styled.header`
  ${props => props.theme.schemaHeader}
`;

export const SchemaExample = styled.div`
  ${props => props.theme.schemaExample}
`;
